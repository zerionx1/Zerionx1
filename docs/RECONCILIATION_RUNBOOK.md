# Reconciliation Runbook

Compare local intent, broker order, fills, cash, margin and positions. Freeze affected connection on unexplained mismatch. Never silently overwrite. Record investigation, resolution owner and timestamp.
