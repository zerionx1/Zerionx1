# Phase 4 APIs

`GET /api/brokers`, `GET|POST /api/execution/intents`, `POST /api/execution/approve`, `GET /api/portfolio`, `GET /api/reconciliation`, and `GET /api/workers/health`. Mutation endpoints intentionally fail closed until production dependencies are configured.
