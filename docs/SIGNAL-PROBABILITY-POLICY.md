# Signal Probability Policy

A confidence value is a model estimate, not the probability of guaranteed profit. Every surfaced signal must include model version, timestamp, expiry, contributing factors, warnings, entry zone, invalidation/stop level and risk-reward estimate. Plans may differ in access and compute limits but must never deliberately degrade model honesty.
