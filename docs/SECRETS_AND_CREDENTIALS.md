# Secrets and Credentials

Never store API secrets in client bundles, GitHub, browser storage or plain database fields. Store encrypted values in a managed vault and persist only vault references. Support rotation and revocation.
