# Optimization Policy

Optimization uses bounded parameter ranges, maximum run limits and distinct training and validation windows. Candidates are ranked with drawdown-aware objectives. Large decay between training and validation is flagged as overfitting. Optimization output is research, not a promise or automatic live authorization.
