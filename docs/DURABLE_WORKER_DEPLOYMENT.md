# Durable Worker Deployment

Deploy the execution worker separately from short-lived web requests. Require persistent queue, retry policy, distributed lock, heartbeat, dead-letter handling, structured logs and alerting. The provider is selected during production infrastructure setup.
