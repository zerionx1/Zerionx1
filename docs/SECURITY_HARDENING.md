# Security Hardening
Use least privilege, MFA for admins, short sessions for sensitive actions, immutable audits, secret rotation, CSP, dependency scanning, rate limits, request validation and encrypted backups. Never expose broker credentials to the browser.
