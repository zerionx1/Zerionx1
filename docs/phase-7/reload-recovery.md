# Reload Recovery
Every data workspace must have loading, empty, error, stale, offline and recovered states. Safe workspace state may restore after reload. Pending live approvals never restore as approved; they must be revalidated server-side.
