"use client"; import { useState } from "react"; import { Button } from "@/components/ui/button"; export function KillSwitchCard(){const [on,setOn]=useState(false);return <section className="rounded-3xl border border-[#E6D8C3] bg-[#F7F4ED] p-6"><h3 className="text-xl font-semibold">Emergency Kill Switch</h3><p className="mt-2 text-sm text-[#2F2A25]">Blocks new automated and paper entries. It does not silently close positions.</p><Button className="mt-5" variant={on?"secondary":"primary"} onClick={()=>setOn(!on)}>{on?"Re-enable entries":"Block all new entries"}</Button></section>}
