"use client"; import { useState } from "react"; import { Button } from "@/components/ui/button"; export function KillSwitchCard(){const [on,setOn]=useState(false);return <section className="rounded-3xl border border-red-300/20 bg-red-950/10 p-6"><h3 className="text-xl font-semibold">Emergency Kill Switch</h3><p className="mt-2 text-sm text-white/55">Blocks new automated and paper entries. It does not silently close positions.</p><Button className="mt-5" variant={on?"secondary":"primary"} onClick={()=>setOn(!on)}>{on?"Re-enable entries":"Block all new entries"}</Button></section>}
