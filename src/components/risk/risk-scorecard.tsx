import type { PortfolioRiskSnapshot } from "@/types/risk"; export function RiskScorecard({snapshot}:{snapshot:PortfolioRiskSnapshot}){const items=[["Risk level",snapshot.level],["Daily loss",`${snapshot.dailyLossPct}%`],["Drawdown",`${snapshot.drawdownPct}%`],["Leverage",`${snapshot.leverage}x`],["Concentration",`${snapshot.concentrationPct}%`],["Open risk",snapshot.openRiskAmount.toLocaleString()]];return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{items.map(([a,b])=><div key={a} className="rounded-3xl border border-[#E6D8C3] bg-[#F7F4ED] p-5"><span className="text-sm text-[#2F2A25]">{a}</span><strong className="mt-2 block text-2xl capitalize">{b}</strong></div>)}</div>}
