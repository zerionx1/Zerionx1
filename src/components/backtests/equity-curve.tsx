import type { EquityPoint } from "@/types/backtest"; export function EquityCurve({points}:{points:EquityPoint[]}){const max=Math.max(...points.map(p=>p.equity),1),min=Math.min(...points.map(p=>p.equity),0);const range=max-min||1;const d=points.map((p,i)=>`${i?"L":"M"} ${(i/Math.max(points.length-1,1))*1000} ${260-((p.equity-min)/range)*230}`).join(" ");return <div className="rounded-3xl border border-[#E6D8C3] bg-[#F7F4ED] p-5"><h3 className="mb-4 font-semibold">Equity curve</h3><svg viewBox="0 0 1000 280" className="w-full"><path d={d} fill="none" stroke="currentColor" strokeWidth="3" className="text-[#2F2A25]"/></svg></div>}
