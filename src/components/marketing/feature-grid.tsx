import { BrainCircuit,ChartNoAxesCombined,LockKeyhole,Settings2,ShieldCheck,Workflow } from "lucide-react"; import { Card } from "@/components/ui/card"; const features=[[BrainCircuit,"Explainable probability","Confidence, evidence, invalidation and model version—not blind calls."],[Workflow,"Visual strategy studio","Compose risk-defined logic without hidden executable code."],[ChartNoAxesCombined,"Paper-first workflow","Research and simulate before any live connection is considered."],[ShieldCheck,"Risk operating system","Position, daily and portfolio limits with safe blocking."],[LockKeyhole,"Explicit authorization","No order without consent or a bounded pre-authorization policy."],[Settings2,"Founder Admin OS","Operate pricing, providers, users and emergency controls visually."]] as const; export function FeatureGrid(){return <section id="platform" className="px-6 py-24"><div className="mx-auto grid max-w-7xl gap-5 md:grid-cols-2 lg:grid-cols-3">{features.map(([Icon,title,copy])=><Card key={title}><Icon className="text-[var(--gold)]"/><h3 className="mt-5 text-xl font-semibold">{title}</h3><p className="mt-3 leading-7 text-[var(--mist)]">{copy}</p></Card>)}</div></section>}