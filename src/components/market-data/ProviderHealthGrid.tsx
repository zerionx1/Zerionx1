import type { FeedHealth } from "@/types/market-data"; export function ProviderHealthGrid({items}:{items:FeedHealth[]}){return <div className="grid gap-3 md:grid-cols-3">{items.length?items.map(x=><article key={x.provider} className="rounded-2xl border border-[#E6D8C3] p-4"><strong>{x.provider}</strong><p>{x.state}</p><small>{x.lagMs===undefined?"No events":`${x.lagMs} ms lag`}</small></article>):<p>No market-data providers configured.</p>}</div>}