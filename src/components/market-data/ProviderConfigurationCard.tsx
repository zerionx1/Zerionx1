export interface ProviderCardData{id:string;enabled:boolean;markets:string[];baseUrlConfigured:boolean;streamUrlConfigured:boolean;} export function ProviderConfigurationCard({provider}:{provider:ProviderCardData}){return <article className="rounded-2xl border border-[#E6D8C3] p-4"><h3>{provider.id}</h3><p>{provider.enabled?"Enabled":"Disabled"}</p><p>{provider.markets.join(", ")||"No markets assigned"}</p><small>REST {provider.baseUrlConfigured?"configured":"missing"} · Stream {provider.streamUrlConfigured?"configured":"missing"}</small></article>}