"use client"; import { useState } from "react"; export function CandleRangeForm(){const[from,setFrom]=useState("");const[to,setTo]=useState("");return <form className="grid gap-3 md:grid-cols-2"><input type="datetime-local" value={from} onChange={e=>setFrom(e.target.value)}/><input type="datetime-local" value={to} onChange={e=>setTo(e.target.value)}/><button type="button" disabled={!from||!to}>Load candles</button></form>}