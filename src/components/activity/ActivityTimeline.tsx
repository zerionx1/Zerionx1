import type { ActivityEvent } from '@/types/activity';
export function ActivityTimeline({items}:{items:ActivityEvent[]}){return <ol className="space-y-3">{items.map((item)=><li key={item.id} className="rounded-xl border border-[#E6D8C3] p-4"><div className="flex justify-between gap-4"><strong>{item.action}</strong><time className="text-xs text-[#2F2A25]">{new Date(item.occurredAt).toLocaleString()}</time></div><p className="mt-1 text-sm text-[#2F2A25]">{item.kind}{item.route?` · ${item.route}`:''}</p></li>)}</ol>;}
