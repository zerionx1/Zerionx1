"use client";import{useEffect}from"react";const KEY="zerion-referral-code-v1";export function ReferralCapture(){useEffect(()=>{const params=new URLSearchParams(window.location.search),incoming=params.get("ref")?.trim();if(incoming)localStorage.setItem(KEY,incoming);const code=incoming||localStorage.getItem(KEY);if(!code)return;let cancelled=false;const claim=async()=>{try{const r=await fetch("/api/referrals/claim",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({code})});if(!cancelled&&r.ok)localStorage.removeItem(KEY)}catch{}};const t=setTimeout(()=>void claim(),900);return()=>{cancelled=true;clearTimeout(t)}},[]);return null}
