"use client";import {useState} from "react";import {Card} from "@/components/ui/card";import {Button} from "@/components/ui/button";export function LiveOrderTicket(){const [confirmed,setConfirmed]=useState(false);return <Card><h3>Live order approval</h3><p>Every AI-suggested order requires your explicit confirmation before submission.</p><label><input type="checkbox" checked={confirmed} onChange={e=>setConfirmed(e.target.checked)}/> I reviewed symbol, quantity, risk and broker account.</label><Button disabled={!confirmed}>Create approval request</Button><small>Live execution remains disabled until a verified broker adapter and production safety checks are enabled.</small></Card>}
