import { AlertTriangle } from "lucide-react"; export function RiskBanner(){return <div className="flex gap-3 rounded-2xl border border-[var(--gold)]/25 bg-[var(--gold)]/5 p-4 text-sm text-[var(--mist)]"><AlertTriangle className="h-5 w-5 shrink-0 text-[var(--gold)]"/><p>Phase 1 workspace uses demonstration state only. No broker is connected and no order can be submitted.</p></div>}