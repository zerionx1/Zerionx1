"use client";
import { Search,Plus,LoaderCircle } from "lucide-react";
import { useEffect,useState } from "react";
import type { MarketInstrument,MarketKind } from "@/types/market";

const filters:[string,MarketKind|""][]=[["All",""],["India","indian-equity"],["Indices","indian-index"],["F&O","indian-futures"],["Crypto","crypto"],["Forex","forex"],["Commodities","commodity"],["US","us-equity"]];
export function GlobalMarketSearch({onAdd}:{onAdd?:(instrument:MarketInstrument)=>void}){const [query,setQuery]=useState("");const [market,setMarket]=useState<MarketKind|"">("");const [rows,setRows]=useState<MarketInstrument[]>([]);const [loading,setLoading]=useState(false);useEffect(()=>{const controller=new AbortController();const timer=setTimeout(async()=>{setLoading(true);try{const response=await fetch(`/api/markets/search?q=${encodeURIComponent(query)}${market?`&market=${market}`:""}`,{signal:controller.signal});const body=await response.json() as {data?:MarketInstrument[]};setRows(body.data??[]);}finally{setLoading(false);}},180);return()=>{clearTimeout(timer);controller.abort();};},[query,market]);return <section className="rounded-[28px] border border-white/10 bg-white/[0.035] p-4 shadow-2xl backdrop-blur-xl"><div className="flex items-center gap-3 rounded-2xl border border-white/10 bg-black/20 px-4"><Search className="h-5 w-5 text-amber-100"/><input value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="Search NSE, BSE, crypto, forex, commodities…" className="h-14 min-w-0 flex-1 bg-transparent text-white outline-none placeholder:text-white/35"/>{loading?<LoaderCircle className="h-5 w-5 animate-spin text-white/50"/>:null}</div><div className="mt-3 flex gap-2 overflow-x-auto pb-1">{filters.map(([label,value])=><button key={label} type="button" onClick={()=>setMarket(value)} className={`shrink-0 rounded-full px-4 py-2 text-xs font-semibold transition ${market===value?"bg-amber-100 text-[#2F2A25]":"border border-white/10 text-white/60 hover:text-white"}`}>{label}</button>)}</div><div className="mt-4 grid gap-2 sm:grid-cols-2 xl:grid-cols-3">{rows.slice(0,12).map((instrument)=><article key={instrument.id} className="flex items-center justify-between gap-3 rounded-2xl border border-white/8 bg-black/15 p-3"><div className="min-w-0"><strong className="block truncate text-sm">{instrument.symbol}</strong><span className="block truncate text-xs text-white/45">{instrument.displayName} · {instrument.exchange}</span></div>{onAdd?<button type="button" aria-label={`Add ${instrument.symbol}`} onClick={()=>onAdd(instrument)} className="rounded-full border border-amber-100/30 p-2 text-amber-100 hover:bg-amber-100/10"><Plus className="h-4 w-4"/></button>:<span className="rounded-full border border-white/10 px-2 py-1 text-[10px] uppercase text-white/40">{instrument.market}</span>}</article>)}</div></section>}
