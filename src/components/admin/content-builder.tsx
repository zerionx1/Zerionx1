"use client";import{useState}from"react";import{Card}from"@/components/ui/card";const blocks=["Hero","Market metrics","Pricing","Video","FAQ","CTA"];export function ContentBuilder(){const[selected,setSelected]=useState<string[]>(["Hero","Market metrics","CTA"]);return <div className="grid gap-4 lg:grid-cols-[260px_1fr]"><Card><p className="mb-3 font-medium">Blocks</p>{blocks.map(b=><button key={b} onClick={()=>setSelected(x=>[...x,b])} className="mb-2 block w-full rounded-lg border border-white/10 p-2 text-left text-sm">+ {b}</button>)}</Card><Card><p className="mb-3 font-medium">Page canvas</p><div className="space-y-3">{selected.map((b,i)=><div draggable key={`${b}-${i}`} className="rounded-xl border border-dashed border-white/20 p-4">{i+1}. {b}</div>)}</div></Card></div>}
