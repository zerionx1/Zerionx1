"use client"; import { useState } from "react"; import { GripVertical,Plus } from "lucide-react"; import { Button } from "@/components/ui/button"; const initial=["Hero announcement","Market status","Risk disclosure","Pricing comparison"]; export function BuilderCanvas(){const [blocks,setBlocks]=useState(initial);return <div className="grid gap-6 lg:grid-cols-[280px_1fr]"><aside className="glass rounded-2xl p-5"><h3 className="font-semibold">Blocks</h3><p className="mt-2 text-sm text-[var(--mist)]">Visual foundation. Persistence and publishing arrive through the Admin OS backend in later phase work.</p><Button className="mt-5 w-full" onClick={()=>setBlocks(v=>[...v,`Custom block ${v.length+1}`])}><Plus className="mr-2 h-4 w-4"/>Add block</Button></aside><section className="min-h-[520px] rounded-2xl border border-dashed border-[var(--gold)]/30 p-5">{blocks.map((b,i)=><div draggable className="mb-3 flex cursor-grab items-center gap-3 rounded-xl border border-white/10 bg-white/[.03] p-4" key={`${b}-${i}`}><GripVertical className="text-[var(--gold)]"/><span>{b}</span></div>)}</section></div>}