"use client"; import { useState } from "react"; import type { FeatureFlag } from "@/types/admin"; import { Card } from "@/components/ui/card"; export function FeatureFlagCard({flag}:{flag:FeatureFlag}){const [enabled,setEnabled]=useState(flag.enabled);return <Card><div className="flex justify-between gap-5"><div><p className="text-xs uppercase tracking-[.2em] text-[var(--gold)]">{flag.risk} risk</p><h3 className="mt-2 font-semibold">{flag.label}</h3><p className="mt-2 text-sm text-[var(--mist)]">{flag.description}</p></div><button onClick={()=>setEnabled(v=>!v)} aria-pressed={enabled} className={`h-7 w-12 rounded-full p-1 transition ${enabled?"bg-[var(--success)]":"bg-white/15"}`}><span className={`block h-5 w-5 rounded-full bg-white transition ${enabled?"translate-x-5":""}`}/></button></div></Card>}