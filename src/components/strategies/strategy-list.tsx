import type { StrategyDefinition } from "@/types/strategy"; import { Badge } from "@/components/ui/badge"; export function StrategyList({items}:{items:StrategyDefinition[]}){return <div className="grid gap-4 lg:grid-cols-2">{items.map(s=><a href={`/dashboard/strategies/${s.id}`} key={s.id} className="rounded-3xl border border-[#E6D8C3] bg-[#F7F4ED] p-6 hover:bg-[#F7F4ED]/[.08]"><div className="flex justify-between"><h3 className="text-xl font-semibold">{s.name}</h3><Badge>{s.status}</Badge></div><p className="mt-3 text-sm text-[#2F2A25]">{s.description}</p><div className="mt-5 flex gap-2 text-xs text-[#2F2A25]"><span>{s.markets.join(" · ")}</span><span>v{s.version}</span><span>{s.nodes.length} nodes</span></div></a>)}</div>}
