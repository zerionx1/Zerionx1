import type { StrategyValidationResult } from "@/types/strategy"; export function ValidationPanel({result}:{result:StrategyValidationResult}){return <section className="rounded-3xl border border-white/10 bg-white/5 p-5"><h3 className="font-semibold">Validation</h3><p className="mt-2 text-sm text-white/55">{result.valid?"Graph is structurally valid.":"Resolve blocking issues before activation."}</p><ul className="mt-4 space-y-2">{result.issues.map(i=><li key={`${i.code}-${i.nodeId}`} className="rounded-xl bg-black/20 p-3 text-sm"><b className="mr-2 uppercase text-amber-100/70">{i.severity}</b>{i.message}</li>)}</ul></section>}
