"use client";import { useRouter } from "next/navigation";import { useState } from "react";import { Button } from "@/components/ui/button";import type { StrategyDefinition } from "@/types/strategy";
export function NewStrategyButton(){const router=useRouter();const [busy,setBusy]=useState(false);async function create(){setBusy(true);const now=new Date().toISOString();const strategy:StrategyDefinition={id:crypto.randomUUID(),ownerId:"current",name:"Untitled Strategy",description:"",markets:["indian-equity"],symbols:[],timeframe:"15m",status:"draft",nodes:[],edges:[],risk:{riskPerTradePct:1,maxDailyLossPct:3,maxOpenPositions:3,minRiskReward:2,stopLossMode:"fixed",takeProfitMode:"risk-multiple"},tags:[],version:1,createdAt:now,updatedAt:now};const r=await fetch("/api/strategies",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(strategy)});const j=await r.json();setBusy(false);if(r.ok)router.push(`/dashboard/strategies/${j.data.strategy.id}`);else alert(j.error?.message??"Unable to create strategy");}return <Button onClick={create} disabled={busy}>{busy?"Creating…":"New strategy"}</Button>}
