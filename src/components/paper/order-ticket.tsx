"use client";
import { useState } from "react";
export function OrderTicket(){const [message,setMessage]=useState("");async function submit(formData:FormData){setMessage("Validating paper order…");const payload=Object.fromEntries(formData);const r=await fetch("/api/paper/orders",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const j=await r.json();setMessage(r.ok?`Paper order ${j.data.status}`:j.error?.message??"Order rejected");}
return <form className="order-ticket" action={submit}><div className="panel-header"><div><p className="eyebrow">Simulation only</p><h2>Paper order ticket</h2></div><span className="data-badge">No real money</span></div><label>Symbol<input name="symbol" defaultValue="NIFTY 50"/></label><input type="hidden" name="market" value="indian-index"/><div className="form-row"><label>Side<select name="side"><option value="buy">Buy</option><option value="sell">Sell</option></select></label><label>Type<select name="type"><option value="market">Market</option><option value="limit">Limit</option></select></label></div><label>Quantity<input name="quantity" type="number" min="1" defaultValue="1"/></label><button type="submit">Place paper order</button>{message&&<p className="form-message" aria-live="polite">{message}</p>}</form>}
