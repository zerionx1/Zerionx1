"use client"; import {FormEvent,useEffect,useMemo,useState} from "react"; import {CheckCircle2,Copy,ShieldCheck,WalletCards} from "lucide-react"; import {planDefinitions} from "@/config/plans"; import type {Plan} from "@/types/entitlements"; export function BillingWorkspace({initialPlan}:{initialPlan?:string}){const[selected,setSelected]=useState<Plan>((initialPlan as Plan)||"starter"),[utr,setUtr]=useState(""),[payerName,setPayerName]=useState(""),[payerUpi,setPayerUpi]=useState(""),[currentPlan,setCurrentPlan]=useState<Plan>("free"),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);const plan=useMemo(()=>planDefinitions.find(p=>p.id===selected)??planDefinitions.find(p=>p.id==="starter")??planDefinitions[0]!,[selected]);const phone=process.env.NEXT_PUBLIC_ZERION_PAYMENT_PHONE||"9019254743",upi=process.env.NEXT_PUBLIC_ZERION_PAYMENT_UPI||"";useEffect(()=>{void fetch("/api/billing/subscription",{cache:"no-store"}).then(r=>r.json()).then(j=>{if(j.data?.plan?.id)setCurrentPlan(j.data.plan.id)})},[]);const amount=plan.launchPriceInr&&plan.launchPriceInr>0?plan.launchPriceInr:plan.monthlyPriceInr;const uri=upi&&amount?`upi://pay?pa=${encodeURIComponent(upi)}&pn=Zerion%20X1&am=${amount}&cu=INR&tn=${encodeURIComponent(`Zerion X1 ${plan.name}`)}`:"";async function submit(e:FormEvent){e.preventDefault();if(!utr.trim()){setMessage("Enter the UTR/reference number after payment.");return}setBusy(true);setMessage("");try{const r=await fetch("/api/billing/payment-request",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({planId:selected,utr,payerName,payerUpi})});const j=await r.json();if(!r.ok)throw new Error(j.error?.message||"Payment request failed");setMessage("Payment submitted for verification. Your plan unlocks after admin approval.");setUtr("")}catch(e){setMessage(e instanceof Error?e.message:"Payment request failed")}finally{setBusy(false)}}return <div className="space-y-6"><section className="zx-billing-summary"><div><p className="eyebrow">Your Zerion plan</p><h1>{planDefinitions.find(p=>p.id===currentPlan)?.name??"Free"}</h1><p>Upgrade whenever you need more markets, AI or automation capacity.</p></div><ShieldCheck/></section><div className="zx-billing-layout"><section className="zx-plan-picker"><p className="eyebrow">Choose plan</p>{planDefinitions.filter(p=>!p.enterprise&&p.id!=="free").map(p=><button type="button" key={p.id} className={selected===p.id?"is-active":""} onClick={()=>setSelected(p.id)}><span><strong>{p.name}</strong><small>{p.tagline}</small></span><b>₹{(p.launchPriceInr||p.monthlyPriceInr)?.toLocaleString("en-IN")}</b></button>)}</section><section className="zx-payment-card"><WalletCards/><p className="eyebrow">Manual UPI payment</p><h2>Upgrade to {plan.name}</h2><p className="zx-payment-amount">₹{amount?.toLocaleString("en-IN")}</p><div className="zx-payment-destination"><span>Payment phone</span><strong>{phone}</strong></div><div className="zx-payment-destination"><span>UPI ID</span><strong>{upi||"Add UPI ID in Zerion billing config"}</strong>{upi?<button type="button" onClick={()=>navigator.clipboard.writeText(upi)}><Copy/></button>:null}</div>{uri?<a className="zx-primary-action" href={uri}>Pay with UPI app</a>:null}<form onSubmit={submit} className="zx-payment-form"><label>UTR / payment reference<input value={utr} onChange={e=>setUtr(e.target.value)} placeholder="Enter UTR after payment"/></label><label>Payer name<input value={payerName} onChange={e=>setPayerName(e.target.value)} placeholder="Name used for payment"/></label><label>Payer UPI ID (optional)<input value={payerUpi} onChange={e=>setPayerUpi(e.target.value)} placeholder="yourname@upi"/></label><button className="zx-primary-action" disabled={busy}>{busy?"Submitting…":"I have paid — send for verification"}</button></form>{message?<div className="zx-billing-message"><CheckCircle2/>{message}</div>:null}</section></div></div>}
