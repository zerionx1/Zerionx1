import Link from "next/link"; import {Check,Sparkles} from "lucide-react"; import {planDefinitions} from "@/config/plans"; export function PricingGrid({dashboard=false}:{dashboard?:boolean}){return <div className="zx-pricing-grid">{planDefinitions.map(p=><article key={p.id} className={`zx-pricing-card ${p.featured?"is-featured":""}`}>{p.featured?<span className="zx-plan-badge">Most balanced</span>:null}<p className="eyebrow">{p.name}</p><h3>{p.tagline}</h3><div className="zx-price-line">{p.enterprise?<strong>Custom</strong>:<><strong>₹{p.monthlyPriceInr?.toLocaleString("en-IN")}</strong><span>/month</span></>}</div>{p.launchPriceInr?<div className="zx-launch-price">Launch: ₹{p.launchPriceInr.toLocaleString("en-IN")} first month</div>:null}<ul>{p.highlights.map(x=><li key={x}><Check/><span>{x}</span></li>)}</ul><div className="zx-plan-examples"><p>What you can ask Zerion</p>{p.simpleExamples.map(x=><span key={x}><Sparkles/>{x}</span>)}</div>{p.enterprise?<Link className="zx-secondary-action" href="/about">Contact Zerion</Link>:dashboard?<Link className="zx-primary-action" href={`/dashboard/billing?plan=${p.id}`}>{p.id==="free"?"Current free access":"Choose plan"}</Link>:<Link className="zx-primary-action" href={`/signup?plan=${p.id}`}>{p.id==="free"?"Start free":"Choose plan"}</Link>}</article>)}</div>}
