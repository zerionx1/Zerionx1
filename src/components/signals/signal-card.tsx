import type { TradingSignal } from "@/types/signal"; import { ConfidenceMeter } from "@/components/signals/confidence-meter";
export function SignalCard({signal}:{signal:TradingSignal}){return <article className="signal-card"><header><div><p className="eyebrow">{signal.market} · {signal.timeframe}</p><h3>{signal.symbol}</h3></div><span className={`bias ${signal.bias}`}>{signal.bias}</span></header><ConfidenceMeter value={signal.confidence}/><div className="signal-levels"><span>Entry {signal.entryZone[0]}–{signal.entryZone[1]}</span><span>SL {signal.stopLoss}</span><span>R:R {signal.riskReward}</span></div><ul>{signal.factors.slice(0,3).map(f=><li key={f.label}><strong>{f.label}</strong><span>{f.explanation}</span></li>)}</ul><small>Educational probability only. No order will be placed.</small></article>}
