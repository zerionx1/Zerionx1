"use client";
import { Check,ChevronDown,GripVertical,LoaderCircle,Plus,RefreshCw,Trash2,X } from "lucide-react";
import { useCallback,useEffect,useMemo,useState } from "react";
import { GlobalMarketSearch } from "@/components/markets/global-market-search";
import type { MarketInstrument,MarketQuote } from "@/types/market";
import type { Watchlist,WatchlistItem } from "@/types/watchlist";

type Api<T>={data?:T;error?:{message?:string}};
export function WatchlistWorkspace(){const [lists,setLists]=useState<Watchlist[]>([]);const [activeId,setActiveId]=useState("");const [quotes,setQuotes]=useState<Record<string,MarketQuote>>({});const [busy,setBusy]=useState(true);const [message,setMessage]=useState("");const [showSearch,setShowSearch]=useState(false);const active=useMemo(()=>lists.find((list)=>list.id===activeId)??lists[0],[lists,activeId]);const load=useCallback(async()=>{setBusy(true);try{const response=await fetch("/api/watchlists",{cache:"no-store"});const body=await response.json() as Api<Watchlist[]>;const next=body.data??[];setLists(next);setActiveId((current)=>current||next[0]?.id||"");}finally{setBusy(false);}},[]);useEffect(()=>{void load();},[load]);useEffect(()=>{if(!active?.items.length){setQuotes({});return;}let mounted=true;Promise.all(active.items.map(async(item)=>{const response=await fetch(`/api/markets/quotes?symbols=${encodeURIComponent(item.symbol)}`,{cache:"no-store"});const body=await response.json() as Api<MarketQuote[]|MarketQuote>;const value=Array.isArray(body.data)?body.data[0]:body.data;return value?[item.symbol,value] as const:null;})).then((values)=>{if(mounted)setQuotes(Object.fromEntries(values.filter((value):value is readonly [string,MarketQuote]=>Boolean(value))));});return()=>{mounted=false;};},[active]);async function createList(){const name=window.prompt("Watchlist name");if(!name?.trim())return;const response=await fetch("/api/watchlists",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,description:"Multi-market watchlist",color:"champagne"})});const body=await response.json() as Api<Watchlist>;if(body.data){setLists((current)=>[...current,body.data!]);setActiveId(body.data.id);}}async function persistItems(items:WatchlistItem[]){if(!active)return;setLists((current)=>current.map((list)=>list.id===active.id?{...list,items}:list));const response=await fetch("/api/watchlists",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:active.id,items})});if(!response.ok){setMessage("Unable to save watchlist");await load();}else setMessage("Saved");}async function addInstrument(instrument:MarketInstrument){if(!active)return;setShowSearch(false);if(active.items.some((item)=>item.instrumentId===instrument.id)){setMessage("Already in this watchlist");return;}await persistItems([...active.items,{id:crypto.randomUUID(),instrumentId:instrument.id,symbol:instrument.symbol,displayName:instrument.displayName,exchange:instrument.exchange,market:instrument.market,addedAt:new Date().toISOString()}]);}async function removeItem(id:string){if(active)await persistItems(active.items.filter((item)=>item.id!==id));}async function deleteList(){if(!active||active.isDefault)return;await fetch(`/api/watchlists?id=${encodeURIComponent(active.id)}`,{method:"DELETE"});setActiveId("");await load();}return <div className="space-y-5"><section className="rounded-[30px] border border-white/10 bg-white/[0.035] p-4"><div className="flex flex-wrap items-center gap-3"><div className="relative min-w-[220px] flex-1"><select value={active?.id??""} onChange={(event)=>setActiveId(event.target.value)} className="h-12 w-full appearance-none rounded-2xl border border-white/10 bg-[#2F2A25] px-4 pr-10 text-sm outline-none">{lists.map((list)=><option key={list.id} value={list.id}>{list.name} ({list.items.length})</option>)}</select><ChevronDown className="pointer-events-none absolute right-3 top-3.5 h-5 w-5 text-white/40"/></div><button type="button" onClick={createList} className="inline-flex h-12 items-center gap-2 rounded-2xl bg-amber-100 px-4 text-sm font-bold text-[#2F2A25]"><Plus className="h-4 w-4"/>New list</button><button type="button" onClick={()=>setShowSearch(true)} disabled={!active} className="inline-flex h-12 items-center gap-2 rounded-2xl border border-white/10 px-4 text-sm text-white/70"><Plus className="h-4 w-4"/>Add symbol</button>{active&&!active.isDefault?<button type="button" onClick={deleteList} className="inline-flex h-12 items-center gap-2 rounded-2xl border border-rose-300/20 px-4 text-sm text-rose-200"><Trash2 className="h-4 w-4"/>Delete</button>:null}</div>{message?<p className="mt-3 flex items-center gap-2 text-xs text-emerald-200"><Check className="h-3.5 w-3.5"/>{message}</p>:null}</section>{showSearch?<div className="fixed inset-0 z-[80] flex items-end bg-black/70 p-3 backdrop-blur-sm sm:items-center sm:justify-center"><div className="max-h-[86vh] w-full max-w-4xl overflow-y-auto rounded-[32px] border border-white/10 bg-[#2F2A25] p-4 shadow-2xl"><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-semibold">Add multi-market instrument</h2><button type="button" onClick={()=>setShowSearch(false)} className="rounded-full border border-white/10 p-2"><X className="h-5 w-5"/></button></div><GlobalMarketSearch onAdd={addInstrument}/></div></div>:null}<section className="overflow-hidden rounded-[30px] border border-white/10 bg-white/[0.035]"><div className="flex items-center justify-between border-b border-white/8 p-5"><div><p className="text-xs uppercase tracking-[.22em] text-amber-100/70">Persistent watchlist</p><h2 className="mt-1 text-xl font-semibold">{active?.name??"Watchlists"}</h2></div><button type="button" onClick={()=>void load()} className="rounded-full border border-white/10 p-2 text-white/50"><RefreshCw className="h-4 w-4"/></button></div>{busy?<div className="flex min-h-52 items-center justify-center"><LoaderCircle className="h-6 w-6 animate-spin text-amber-100"/></div>:!active?.items.length?<div className="p-10 text-center"><p className="text-white/50">No instruments yet.</p><button type="button" onClick={()=>setShowSearch(true)} className="mt-4 rounded-full bg-amber-100 px-5 py-2.5 text-sm font-bold text-[#2F2A25]">Add your first symbol</button></div>:<div className="divide-y divide-white/7">{active.items.map((item)=><article key={item.id} className="grid grid-cols-[auto_1fr_auto] items-center gap-3 p-4 hover:bg-white/[0.025]"><GripVertical className="h-4 w-4 text-white/20"/><div className="min-w-0"><div className="flex items-center gap-2"><strong>{item.symbol}</strong><span className="rounded-full bg-white/5 px-2 py-1 text-[10px] text-white/40">{item.exchange??item.market}</span></div><p className="truncate text-xs text-white/40">{item.displayName??item.instrumentId}</p></div><div className="flex items-center gap-3">{quotes[item.symbol]?<div className="text-right"><strong>{quotes[item.symbol]!.price.toLocaleString()}</strong><p className={`text-xs ${quotes[item.symbol]!.changePercent>=0?"text-emerald-300":"text-rose-300"}`}>{quotes[item.symbol]!.changePercent.toFixed(2)}%</p></div>:<span className="text-xs text-white/35">Provider required</span>}<button type="button" onClick={()=>void removeItem(item.id)} className="rounded-full border border-white/8 p-2 text-white/35 hover:text-rose-200"><Trash2 className="h-4 w-4"/></button></div></article>)}</div>}</section></div>}
