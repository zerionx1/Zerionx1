import type { MarketDataProvider } from "@/lib/market-data/core/provider"; import { SymbolRepository } from "@/lib/market-data/symbols/repository"; export class SymbolCatalogService{constructor(private repo:SymbolRepository){}async refresh(providers:MarketDataProvider[]){const settled=await Promise.allSettled(providers.map(p=>p.symbols()));const symbols=settled.flatMap(r=>r.status==="fulfilled"?r.value:[]);for(const symbol of symbols)this.repo.upsert(symbol);return{providers:providers.length,symbols:this.repo.list().length,failed:settled.filter(x=>x.status==="rejected").length};}}