import { MarketingHeader } from "@/components/marketing/header"; import { Card } from "@/components/ui/card"; const items=["Step-up authentication for sensitive actions","No secrets in client bundles","Role and permission boundaries","Idempotent money-sensitive intent design","Tamper-evident audit contracts","Safe failure when data is stale or unavailable"]; export default function Security(){return <><MarketingHeader/><main className="mx-auto max-w-5xl px-6 pb-24 pt-36"><h1 className="text-5xl font-semibold">Financial safety is a <span className="gold-text">system property.</span></h1><div className="mt-10 grid gap-4 md:grid-cols-2">{items.map(x=><Card key={x}>{x}</Card>)}</div></main></>}