import { StrategyList } from "@/components/strategies/strategy-list";import { listUserStrategies } from "@/lib/strategy/strategy-repository";import { NewStrategyButton } from "@/components/strategies/new-strategy-button";
export default async function Page(){const strategies=await listUserStrategies();return <><div className="mb-8 flex items-end justify-between"><div><p className="text-sm uppercase tracking-[.25em] text-amber-100/50">Strategy workspace</p><h1 className="mt-2 text-4xl font-semibold">Strategy Studio</h1><p className="mt-2 text-white/55">Build, save, validate and version user-owned strategies.</p></div><NewStrategyButton/></div><StrategyList items={strategies}/></>}
