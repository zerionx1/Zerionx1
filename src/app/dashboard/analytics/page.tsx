import { LineChart } from "@/components/charts/line-chart";import { createSampleCandles } from "@/lib/market/sample-data";import { summariseTrades } from "@/lib/analytics/performance";
export default function AnalyticsPage(){const metrics=summariseTrades([4200,-1800,3100,900,-700,2500]);return <main className="dashboard-page"><div className="page-heading"><div><p className="eyebrow">Trading behaviour</p><h1>Performance Analytics</h1><p>Paper-account metrics designed to expose risk and consistency, not only returns.</p></div></div><div className="stat-grid">{[["Net P&L",metrics.netPnl],["Win rate",`${metrics.winRate.toFixed(1)}%`],["Profit factor",metrics.profitFactor.toFixed(2)],["Expectancy",metrics.expectancy.toFixed(0)]].map(([k,v])=><div className="stat-tile" key={String(k)}><span>{k}</span><strong>{v}</strong></div>)}</div><section className="panel"><div className="panel-header"><h2>Paper equity curve</h2><span className="data-badge">Illustrative</span></div><LineChart candles={createSampleCandles(1000000,40)}/></section></main>}
