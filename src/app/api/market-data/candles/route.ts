import { NextRequest,NextResponse } from "next/server"; export async function GET(request:NextRequest){const p=request.nextUrl.searchParams;const required=["provider","symbol","timeframe","from","to"];const missing=required.filter(k=>!p.get(k));if(missing.length)return NextResponse.json({error:"Missing parameters",missing},{status:400});return NextResponse.json({candles:[],status:"provider_not_connected"},{status:503});}