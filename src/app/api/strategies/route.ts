import { NextRequest } from "next/server";import { apiError,apiSuccess } from "@/lib/security/api-response";import { listUserStrategies,saveUserStrategy } from "@/lib/strategy/strategy-repository";import { strategySchema } from "@/lib/validation/strategy";
export async function GET(){return apiSuccess({strategies:await listUserStrategies()})}
export async function POST(req:NextRequest){const parsed=strategySchema.safeParse(await req.json());if(!parsed.success)return apiError("VALIDATION_ERROR","Invalid strategy definition",400,parsed.error.flatten());return apiSuccess({strategy:await saveUserStrategy(parsed.data)},201)}
