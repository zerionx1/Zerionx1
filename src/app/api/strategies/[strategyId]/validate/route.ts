import { apiError,apiSuccess } from "@/lib/security/api-response";import { getUserStrategy } from "@/lib/strategy/strategy-repository";import { validateStrategy } from "@/lib/strategy/validator";
export async function POST(_:Request,{params}:{params:Promise<{strategyId:string}>}){const {strategyId}=await params;const strategy=await getUserStrategy(strategyId);return strategy?apiSuccess({validation:validateStrategy(strategy)}):apiError("NOT_FOUND","Strategy not found",404)}
