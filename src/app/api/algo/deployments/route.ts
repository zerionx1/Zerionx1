import { currentUser, insert, select } from "@/lib/supabase/rest";
import { fail, ok } from "@/lib/security/api-response";
export async function GET(){try{const user=await currentUser();const rows=await select("algo_deployments",`owner_id=eq.${user.id}&order=created_at.desc`);return ok({deployments:rows})}catch(e){return fail("DEPLOYMENTS_READ_FAILED",e instanceof Error?e.message:"Unable to load deployments",400)}}
export async function POST(request:Request){try{const user=await currentUser();const body=await request.json();if(!body.name||!body.strategyId||!body.mode||!body.market||!body.symbol||!Number(body.capital))return fail("VALIDATION_ERROR","Name, strategy, mode, market, symbol and positive capital are required",400);if(!["paper","live"].includes(body.mode))return fail("VALIDATION_ERROR","Invalid deployment mode",400);const rows=await insert<Record<string,unknown>>("algo_deployments",{owner_id:user.id,name:String(body.name),strategy_id:String(body.strategyId),mode:body.mode,market:String(body.market),symbol:String(body.symbol),capital:Number(body.capital),status:"paused",risk_config:body.riskConfig??{}});return ok({deployment:rows[0]},201)}catch(e){return fail("DEPLOYMENT_CREATE_FAILED",e instanceof Error?e.message:"Unable to create deployment",400)}}
