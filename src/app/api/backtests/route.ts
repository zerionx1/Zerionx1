import { NextRequest } from "next/server"; import { apiError, apiSuccess } from "@/lib/security/api-response"; import { backtestRequestSchema } from "@/lib/validation/backtest"; import { listBacktests, saveBacktest } from "@/lib/backtest/backtest-store"; import { runEducationalBacktest } from "@/lib/backtest/simulator"; import { getCandles } from "@/lib/market/sample-data";
export async function GET(){return apiSuccess({backtests:listBacktests()})}
export async function POST(req:NextRequest){const parsed=backtestRequestSchema.safeParse(await req.json());if(!parsed.success)return apiError("VALIDATION_ERROR","Invalid backtest request",400,parsed.error.flatten());const request={...parsed.data,id:`bt_${crypto.randomUUID()}`};const candles=getCandles(request.symbol,request.timeframe,300);return apiSuccess({result:saveBacktest(runEducationalBacktest(request,candles))},201)}
