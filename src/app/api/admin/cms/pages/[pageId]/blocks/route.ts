import {requirePermission} from "@/lib/auth/require-permission"; import {createCmsBlock} from "@/lib/cms/cms-v2-service"; import {ok,fail} from "@/lib/security/api-response"; import type {CmsBlockType} from "@/types/cms-v2"; const allowed=new Set<CmsBlockType>(["hero","rich_text","feature_grid","pricing","cta","notice","stats","faq"]); export async function POST(r:Request,c:{params:Promise<{pageId:string}>}){await requirePermission("admin.read");const{pageId}=await c.params;const b=await r.json().catch(()=>null) as {blockType?:CmsBlockType;position?:number}|null;if(!b?.blockType||!allowed.has(b.blockType))return fail("VALIDATION_ERROR","Unsupported block type",400);return ok(await createCmsBlock(pageId,b.blockType,Number.isFinite(b.position)?Number(b.position):0))}
