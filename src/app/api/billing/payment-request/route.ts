import {requestPlanUpgrade} from "@/lib/billing/plan-service"; import {ok,fail} from "@/lib/security/api-response"; import type {Plan} from "@/types/entitlements"; export async function POST(request:Request){const b=await request.json().catch(()=>null) as {planId?:Plan;utr?:string;payerName?:string;payerUpi?:string}|null;if(!b?.planId||!b.utr?.trim())return fail("VALIDATION_ERROR","Plan and UTR/reference number are required",400);try{return ok(await requestPlanUpgrade({planId:b.planId,utr:b.utr,payerName:b.payerName,payerUpi:b.payerUpi}))}catch(e){return fail("PAYMENT_REQUEST_FAILED",e instanceof Error?e.message:"Could not submit payment",400)}}
