import { getFoundationHealth } from "@/lib/observability/health"; import { Card } from "@/components/ui/card"; export default async function Health(){const checks=await getFoundationHealth();return <><h1 className="mb-6 text-4xl font-semibold">System Health</h1><div className="grid gap-4 md:grid-cols-3">{checks.map(c=><Card key={c.name}><p className="text-sm text-[var(--mist)]">{c.name}</p><p className="mt-2 text-2xl capitalize">{c.status}</p><p className="mt-2 text-xs text-[var(--gold)]">{c.latencyMs} ms</p></Card>)}</div></>}