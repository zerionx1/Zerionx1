import { FeatureFlagCard } from "@/components/admin/feature-flag-card"; import type { FeatureFlag } from "@/types/admin"; const flags:FeatureFlag[]=[{key:"paper_mode",label:"Paper-safe mode",description:"Allows simulation workflows without broker execution.",enabled:true,risk:"low"},{key:"live_execution",label:"Live execution",description:"Global gate for broker order submission.",enabled:false,risk:"high"},{key:"community",label:"Community",description:"Enables private group surfaces after moderation controls.",enabled:false,risk:"medium"}]; export default function Features(){return <><h1 className="mb-6 text-4xl font-semibold">Feature Control</h1><div className="grid gap-4 lg:grid-cols-2">{flags.map(f=><FeatureFlagCard flag={f} key={f.key}/>)}</div></>}