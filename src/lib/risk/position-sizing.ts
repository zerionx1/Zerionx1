import type { PositionSizingInput, PositionSizingResult } from "@/types/risk";
export function calculatePositionSize(input:PositionSizingInput):PositionSizingResult{const warnings:string[]=[];const multiplier=input.contractMultiplier??1;const riskAmount=Math.max(0,input.equity*(input.riskPct/100));const riskPerUnit=Math.abs(input.entryPrice-input.stopPrice)*multiplier;if(!Number.isFinite(riskPerUnit)||riskPerUnit<=0)return{riskAmount,riskPerUnit:0,rawQuantity:0,allowedQuantity:0,notional:0,notionalPct:0,warnings:["Entry and stop prices must define a positive risk distance."]};const rawQuantity=Math.floor(riskAmount/riskPerUnit);const maxNotional=input.equity*((input.maxPositionPct??20)/100);const maxQty=Math.floor(maxNotional/(input.entryPrice*multiplier));const allowedQuantity=Math.max(0,Math.min(rawQuantity,maxQty));if(allowedQuantity<rawQuantity)warnings.push("Quantity reduced by maximum position allocation.");if(allowedQuantity===0)warnings.push("No position is allowed under current limits.");const notional=allowedQuantity*input.entryPrice*multiplier;return{riskAmount,riskPerUnit,rawQuantity,allowedQuantity,notional,notionalPct:input.equity?notional/input.equity*100:0,warnings}}
