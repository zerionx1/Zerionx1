import type { MarketDataProvider } from "./provider"; export class ProviderRegistry{private providers=new Map<string,MarketDataProvider>();register(provider:MarketDataProvider){if(this.providers.has(provider.id))throw new Error(`Provider already registered: ${provider.id}`);this.providers.set(provider.id,provider);}get(id:string){return this.providers.get(id);}list(){return [...this.providers.values()];}enabled(){return this.list().filter(p=>p.enabled);}}