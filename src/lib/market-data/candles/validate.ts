import type { MarketCandle } from "@/types/market-data"; export const validateCandle=(c:MarketCandle)=>({valid:c.high>=Math.max(c.open,c.close)&&c.low<=Math.min(c.open,c.close)&&c.volume>=0&&c.openTime<c.closeTime,errors:[c.high<Math.max(c.open,c.close)?"high below body":null,c.low>Math.min(c.open,c.close)?"low above body":null,c.volume<0?"negative volume":null].filter(Boolean) as string[]});