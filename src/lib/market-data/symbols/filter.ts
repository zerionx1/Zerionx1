import type { MarketSymbol,MarketAssetClass } from "@/types/market-data"; export interface SymbolFilter{assetClasses?:MarketAssetClass[];venues?:string[];currency?:string;enabled?:boolean;} export const filterSymbols=(items:MarketSymbol[],f:SymbolFilter)=>items.filter(x=>(!f.assetClasses||f.assetClasses.includes(x.assetClass))&&(!f.venues||f.venues.includes(x.venue))&&(!f.currency||x.currency===f.currency)&&(f.enabled===undefined||x.enabled===f.enabled));