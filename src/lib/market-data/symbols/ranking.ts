import type { MarketSymbol } from "@/types/market-data"; export const rankSymbolSearch=(items:MarketSymbol[],query:string)=>{const q=query.toLowerCase();return [...items].sort((a,b)=>score(b)-score(a));function score(x:MarketSymbol){const s=x.canonicalSymbol.toLowerCase(),n=x.displayName.toLowerCase();return s===q?100:s.startsWith(q)?80:n.startsWith(q)?60:s.includes(q)?40:n.includes(q)?20:0;}};