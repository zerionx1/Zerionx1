import type { MarketSymbol } from "@/types/market-data"; export const validateSymbol=(s:MarketSymbol)=>{const errors:string[]=[];if(!s.id)errors.push("id required");if(!s.canonicalSymbol)errors.push("canonical symbol required");if(s.tickSize<=0)errors.push("tick size must be positive");if(s.lotSize<=0)errors.push("lot size must be positive");if(s.priceScale<0)errors.push("price scale cannot be negative");return{valid:errors.length===0,errors};};