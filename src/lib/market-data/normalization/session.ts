export interface SessionNormalizationInput { value: unknown; provider: string; receivedAt?: number; }
export interface SessionNormalizationResult<T> { ok:boolean; value?:T; errors:string[]; provider:string; }
export function normalizeSession<T>(input:SessionNormalizationInput, mapper:(value:unknown)=>T):SessionNormalizationResult<T>{try{const value=mapper(input.value);return{ok:true,value,errors:[],provider:input.provider};}catch(error){return{ok:false,errors:[error instanceof Error?error.message:"Normalization failed"],provider:input.provider};}}