export interface TimezoneNormalizationInput { value: unknown; provider: string; receivedAt?: number; }
export interface TimezoneNormalizationResult<T> { ok:boolean; value?:T; errors:string[]; provider:string; }
export function normalizeTimezone<T>(input:TimezoneNormalizationInput, mapper:(value:unknown)=>T):TimezoneNormalizationResult<T>{try{const value=mapper(input.value);return{ok:true,value,errors:[],provider:input.provider};}catch(error){return{ok:false,errors:[error instanceof Error?error.message:"Normalization failed"],provider:input.provider};}}