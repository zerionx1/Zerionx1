export interface CandleNormalizationInput { value: unknown; provider: string; receivedAt?: number; }
export interface CandleNormalizationResult<T> { ok:boolean; value?:T; errors:string[]; provider:string; }
export function normalizeCandle<T>(input:CandleNormalizationInput, mapper:(value:unknown)=>T):CandleNormalizationResult<T>{try{const value=mapper(input.value);return{ok:true,value,errors:[],provider:input.provider};}catch(error){return{ok:false,errors:[error instanceof Error?error.message:"Normalization failed"],provider:input.provider};}}