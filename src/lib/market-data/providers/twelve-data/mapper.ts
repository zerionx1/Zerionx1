import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface TwelveDataMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedTwelveDataMapper:TwelveDataMapper={symbol:()=>{throw new Error("twelve-data symbol mapper not configured")},tick:()=>{throw new Error("twelve-data tick mapper not configured")},candle:()=>{throw new Error("twelve-data candle mapper not configured")}};