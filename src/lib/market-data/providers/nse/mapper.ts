import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface NseMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedNseMapper:NseMapper={symbol:()=>{throw new Error("nse symbol mapper not configured")},tick:()=>{throw new Error("nse tick mapper not configured")},candle:()=>{throw new Error("nse candle mapper not configured")}};