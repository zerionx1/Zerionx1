import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface AngelOneMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedAngelOneMapper:AngelOneMapper={symbol:()=>{throw new Error("angel-one symbol mapper not configured")},tick:()=>{throw new Error("angel-one tick mapper not configured")},candle:()=>{throw new Error("angel-one candle mapper not configured")}};