import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface CoinbaseMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedCoinbaseMapper:CoinbaseMapper={symbol:()=>{throw new Error("coinbase symbol mapper not configured")},tick:()=>{throw new Error("coinbase tick mapper not configured")},candle:()=>{throw new Error("coinbase candle mapper not configured")}};