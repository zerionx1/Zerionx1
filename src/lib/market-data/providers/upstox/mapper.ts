import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface UpstoxMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedUpstoxMapper:UpstoxMapper={symbol:()=>{throw new Error("upstox symbol mapper not configured")},tick:()=>{throw new Error("upstox tick mapper not configured")},candle:()=>{throw new Error("upstox candle mapper not configured")}};