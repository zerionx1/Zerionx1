import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface BseMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedBseMapper:BseMapper={symbol:()=>{throw new Error("bse symbol mapper not configured")},tick:()=>{throw new Error("bse tick mapper not configured")},candle:()=>{throw new Error("bse candle mapper not configured")}};