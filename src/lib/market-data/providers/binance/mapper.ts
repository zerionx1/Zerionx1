import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface BinanceMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedBinanceMapper:BinanceMapper={symbol:()=>{throw new Error("binance symbol mapper not configured")},tick:()=>{throw new Error("binance tick mapper not configured")},candle:()=>{throw new Error("binance candle mapper not configured")}};