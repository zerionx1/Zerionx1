import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface GenericForexMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedGenericForexMapper:GenericForexMapper={symbol:()=>{throw new Error("generic-forex symbol mapper not configured")},tick:()=>{throw new Error("generic-forex tick mapper not configured")},candle:()=>{throw new Error("generic-forex candle mapper not configured")}};