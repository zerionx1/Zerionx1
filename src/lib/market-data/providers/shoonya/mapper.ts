import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface ShoonyaMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedShoonyaMapper:ShoonyaMapper={symbol:()=>{throw new Error("shoonya symbol mapper not configured")},tick:()=>{throw new Error("shoonya tick mapper not configured")},candle:()=>{throw new Error("shoonya candle mapper not configured")}};