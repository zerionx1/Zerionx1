import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface OandaMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedOandaMapper:OandaMapper={symbol:()=>{throw new Error("oanda symbol mapper not configured")},tick:()=>{throw new Error("oanda tick mapper not configured")},candle:()=>{throw new Error("oanda candle mapper not configured")}};