import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface ZerodhaMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedZerodhaMapper:ZerodhaMapper={symbol:()=>{throw new Error("zerodha symbol mapper not configured")},tick:()=>{throw new Error("zerodha tick mapper not configured")},candle:()=>{throw new Error("zerodha candle mapper not configured")}};