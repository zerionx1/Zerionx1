import type { FeedHealth,MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; import type { MarketDataProvider,ProviderCredentials } from "../../core/provider"; import type { OkxProviderConfig } from "./config"; import { OkxHttpClient } from "./client"; import type { OkxMapper } from "./mapper"; export class OkxMarketDataProvider implements MarketDataProvider{readonly id="okx";readonly displayName="Okx";private state:FeedHealth={provider:this.id,state:"idle",reconnects:0,droppedEvents:0,invalidEvents:0};private client:OkxHttpClient;constructor(readonly config:OkxProviderConfig,private mapper:OkxMapper){this.client=new OkxHttpClient(config);}get enabled(){return this.config.enabled;}async connect(_credentials?:ProviderCredentials){if(!this.enabled)throw new Error("okx provider disabled");this.state={...this.state,state:"connected"};}async disconnect(){this.state={...this.state,state:"disconnected"};}health(){return{...this.state};}async symbols():Promise<MarketSymbol[]>{const rows=await this.client.request<unknown[]>("/symbols");return rows.map(this.mapper.symbol);}async historical(symbol:string,timeframe:Timeframe,from:number,to:number):Promise<MarketCandle[]>{const rows=await this.client.request<unknown[]>(`/candles?symbol=${encodeURIComponent(symbol)}&timeframe=${timeframe}&from=${from}&to=${to}`);return rows.map(row=>this.mapper.candle(row,timeframe));}async subscribe(_symbols:string[],_onTick:(tick:MarketTick)=>void):Promise<()=>Promise<void>>{if(!this.config.streamUrl)throw new Error("okx stream URL is not configured");throw new Error("Streaming transport must be supplied by the deployment adapter");}}