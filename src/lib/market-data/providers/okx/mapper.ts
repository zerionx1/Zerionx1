import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface OkxMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedOkxMapper:OkxMapper={symbol:()=>{throw new Error("okx symbol mapper not configured")},tick:()=>{throw new Error("okx tick mapper not configured")},candle:()=>{throw new Error("okx candle mapper not configured")}};