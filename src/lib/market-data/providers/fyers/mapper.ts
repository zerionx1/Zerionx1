import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface FyersMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedFyersMapper:FyersMapper={symbol:()=>{throw new Error("fyers symbol mapper not configured")},tick:()=>{throw new Error("fyers tick mapper not configured")},candle:()=>{throw new Error("fyers candle mapper not configured")}};