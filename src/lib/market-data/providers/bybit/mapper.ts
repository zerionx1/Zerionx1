import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface BybitMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedBybitMapper:BybitMapper={symbol:()=>{throw new Error("bybit symbol mapper not configured")},tick:()=>{throw new Error("bybit tick mapper not configured")},candle:()=>{throw new Error("bybit candle mapper not configured")}};