import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface KrakenMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedKrakenMapper:KrakenMapper={symbol:()=>{throw new Error("kraken symbol mapper not configured")},tick:()=>{throw new Error("kraken tick mapper not configured")},candle:()=>{throw new Error("kraken candle mapper not configured")}};