import type { MarketCandle,MarketSymbol,MarketTick,Timeframe } from "@/types/market-data"; export interface McxMapper{symbol(raw:unknown):MarketSymbol;tick(raw:unknown):MarketTick;candle(raw:unknown,timeframe:Timeframe):MarketCandle;} export const unsupportedMcxMapper:McxMapper={symbol:()=>{throw new Error("mcx symbol mapper not configured")},tick:()=>{throw new Error("mcx tick mapper not configured")},candle:()=>{throw new Error("mcx candle mapper not configured")}};