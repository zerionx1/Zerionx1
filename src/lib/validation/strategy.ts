import { z } from "zod";
export const strategyNodeSchema=z.object({id:z.string().min(1),kind:z.enum(["source","indicator","condition","risk","entry","exit","logic"]),label:z.string().min(1).max(80),x:z.number(),y:z.number(),config:z.record(z.string(),z.union([z.string(),z.number(),z.boolean()]))});
export const strategySchema=z.object({id:z.string().min(1),ownerId:z.string().min(1),name:z.string().min(3).max(100),description:z.string().max(500),markets:z.array(z.enum(["indian-equity","indian-index","crypto","forex"])).min(1),symbols:z.array(z.string()).min(1).max(25),timeframe:z.enum(["1m","5m","15m","30m","1h","4h","1d"]),status:z.enum(["draft","validated","paper-ready","archived"]),nodes:z.array(strategyNodeSchema).max(80),edges:z.array(z.object({id:z.string(),source:z.string(),target:z.string(),sourceHandle:z.string().optional(),targetHandle:z.string().optional()})).max(140),risk:z.object({riskPerTradePct:z.number().positive().max(5),maxDailyLossPct:z.number().positive().max(10),maxOpenPositions:z.number().int().positive().max(25),minRiskReward:z.number().positive(),stopLossMode:z.enum(["fixed","atr","structure"]),takeProfitMode:z.enum(["fixed","risk-multiple","trailing"])}),tags:z.array(z.string()).max(10),version:z.number().int().positive(),createdAt:z.string(),updatedAt:z.string()});
