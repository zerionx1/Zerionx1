import { z } from "zod";
export const backtestRequestSchema=z.object({strategyId:z.string().min(1),strategyVersion:z.number().int().positive(),market:z.enum(["indian-equity","indian-index","crypto","forex"]),symbol:z.string().min(1),timeframe:z.enum(["1m","5m","15m","30m","1h","4h","1d"]),startDate:z.string().min(10),endDate:z.string().min(10),assumptions:z.object({initialCapital:z.number().positive(),commissionBps:z.number().min(0).max(500),slippageBps:z.number().min(0).max(500),latencyMs:z.number().min(0).max(60000),allowShort:z.boolean(),maxPositionPct:z.number().positive().max(100)})});
