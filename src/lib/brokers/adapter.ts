import type { BrokerConnection } from "@/types/broker"; import type { ExecutionReceipt,LiveOrderIntent } from "@/types/execution"; import type { PortfolioSnapshot } from "@/types/portfolio";
export interface BrokerAdapter { connect(input:Record<string,string>):Promise<BrokerConnection>; disconnect(connectionId:string):Promise<void>; validate(connection:BrokerConnection):Promise<boolean>; submitOrder(connection:BrokerConnection,intent:LiveOrderIntent):Promise<ExecutionReceipt>; cancelOrder(connection:BrokerConnection,brokerOrderId:string):Promise<ExecutionReceipt>; getPortfolio(connection:BrokerConnection):Promise<PortfolioSnapshot>; }
