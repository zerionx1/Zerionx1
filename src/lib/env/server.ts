import "server-only"; import { z } from "zod"; const schema=z.object({NODE_ENV:z.enum(["development","test","production"]).default("development"),AUTH_SECRET:z.string().min(32).default("development-only-secret-change-me-now"),DATABASE_URL:z.string().optional(),ENCRYPTION_KEY:z.string().optional(),OBSERVABILITY_DSN:z.string().optional()}); export const serverEnv=schema.parse(process.env);