import type {ReconciliationItem} from "@/types/reconciliation"; import type {ExecutionReceipt} from "@/types/execution"; export const reconcileOrders=(local:ExecutionReceipt[],remote:ExecutionReceipt[]):ReconciliationItem[]=>{const out:ReconciliationItem[]=[];const remoteBy=new Map(remote.map(x=>[x.brokerOrderId,x]));for(const l of local){const r=l.brokerOrderId?remoteBy.get(l.brokerOrderId):undefined;if(!r)out.push({id:`rec-${l.intentId}`,connectionId:"unknown",localOrderId:l.intentId,brokerOrderId:l.brokerOrderId,state:"missing-broker",details:"Local order is absent from broker snapshot",detectedAt:new Date().toISOString()});else if(l.status!==r.status)out.push({id:`rec-${l.intentId}`,connectionId:"unknown",localOrderId:l.intentId,brokerOrderId:l.brokerOrderId,state:"status-mismatch",details:`Local ${l.status}; broker ${r.status}`,detectedAt:new Date().toISOString()});}return out;};
