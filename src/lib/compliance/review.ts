export type ComplianceDecision="approve"|"reject"|"needs_changes";export interface ComplianceReview{resourceType:string;resourceId:string;decision:ComplianceDecision;reason:string;reviewerId:string;reviewedAt:string}export function createComplianceReview(input:Omit<ComplianceReview,"reviewedAt">):ComplianceReview{if(input.reason.trim().length<10)throw new Error("Detailed compliance reason required");return{...input,reviewedAt:new Date().toISOString()}}
