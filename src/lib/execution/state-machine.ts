import type {OrderStatus} from "@/types/execution"; const allowed:Record<OrderStatus,OrderStatus[]>={draft:["awaiting-confirmation","cancelled"],"awaiting-confirmation":["approved","cancelled","expired"],approved:["queued","cancelled"],queued:["submitted","rejected","cancelled"],submitted:["partially-filled","filled","cancelled","rejected","reconciliation-required"],"partially-filled":["filled","cancelled","reconciliation-required"],filled:[],cancelled:[],rejected:[],expired:[],"reconciliation-required":["filled","cancelled","rejected"]}; export const assertOrderTransition=(from:OrderStatus,to:OrderStatus)=>{if(!allowed[from].includes(to)) throw new Error(`Invalid order transition: ${from} -> ${to}`);};
