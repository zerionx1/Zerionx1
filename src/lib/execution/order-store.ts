import type {LiveOrderIntent,ExecutionReceipt} from "@/types/execution"; const intents=new Map<string,LiveOrderIntent>(); const receipts=new Map<string,ExecutionReceipt>(); export const liveOrderStore={list:(userId:string)=>[...intents.values()].filter(x=>x.userId===userId),get:(id:string)=>intents.get(id),save:(x:LiveOrderIntent)=>(intents.set(x.id,x),x),saveReceipt:(x:ExecutionReceipt)=>(receipts.set(x.intentId,x),x),receipt:(id:string)=>receipts.get(id)};
