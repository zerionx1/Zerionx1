import type {LiveOrderIntent} from "@/types/execution"; import {executionPolicy} from "@/config/execution"; export interface PreflightContext{quoteTimestamp:number;killSwitch:boolean;availableMargin:number;estimatedCost:number;connectionHealthy:boolean;} export const runExecutionPreflight=(intent:LiveOrderIntent,c:PreflightContext)=>{const errors:string[]=[]; if(!executionPolicy.allowLiveExecution) errors.push("Live execution feature flag is disabled"); if(c.killSwitch) errors.push("Emergency kill switch is active"); if(!c.connectionHealthy) errors.push("Broker connection is unhealthy"); if(Date.now()-c.quoteTimestamp>executionPolicy.staleQuoteMs) errors.push("Market quote is stale"); if(c.estimatedCost>c.availableMargin) errors.push("Insufficient available margin"); if(intent.quantity<=0) errors.push("Quantity must be positive"); return {ok:errors.length===0,errors};};
