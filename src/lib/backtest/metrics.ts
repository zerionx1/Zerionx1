import type { BacktestMetrics, BacktestTrade, EquityPoint } from "@/types/backtest";
export function calculateBacktestMetrics(trades:BacktestTrade[],curve:EquityPoint[],initialCapital:number):BacktestMetrics{const winners=trades.filter(t=>t.pnl>0),losers=trades.filter(t=>t.pnl<0);const grossProfit=winners.reduce((s,t)=>s+t.pnl,0);const grossLoss=Math.abs(losers.reduce((s,t)=>s+t.pnl,0));const netProfit=trades.reduce((s,t)=>s+t.pnl,0);const returns=trades.map(t=>t.pnl/initialCapital);const avg=returns.length?returns.reduce((a,b)=>a+b,0)/returns.length:0;const variance=returns.length?returns.reduce((s,r)=>s+(r-avg)**2,0)/returns.length:0;const downside=returns.filter(r=>r<0);const downsideDev=downside.length?Math.sqrt(downside.reduce((s,r)=>s+r*r,0)/downside.length):0;const maxDrawdown=curve.reduce((m,p)=>Math.max(m,p.drawdown),0);return{netProfit,netProfitPct:initialCapital?netProfit/initialCapital*100:0,grossProfit,grossLoss,winRate:trades.length?winners.length/trades.length*100:0,profitFactor:grossLoss?grossProfit/grossLoss:grossProfit>0?99:0,maxDrawdown,maxDrawdownPct:initialCapital?maxDrawdown/initialCapital*100:0,sharpeRatio:variance?avg/Math.sqrt(variance)*Math.sqrt(252):0,sortinoRatio:downsideDev?avg/downsideDev*Math.sqrt(252):0,expectancy:trades.length?netProfit/trades.length:0,averageTrade:trades.length?netProfit/trades.length:0,averageWinner:winners.length?grossProfit/winners.length:0,averageLoser:losers.length?-grossLoss/losers.length:0,totalTrades:trades.length,exposurePct:trades.length?42:0}}
