export const migration={id:'0007-create-signals',up:["create table if not exists signals (id text primary key, tenant_id text not null, payload jsonb not null, created_at timestamptz not null, updated_at timestamptz not null)"],down:["drop table if exists signals"]} as const;
