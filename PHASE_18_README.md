# Phase 18
Full global UI reset using the UI/UX Pro Max reference principles.
Important: UI/UX Pro Max is a design-time CLI/skill, not a browser CSS package. INSTALL_UI_UX_PRO_MAX.sh installs the official current CLI and initializes the universal skill. Runtime styling is implemented in phase-18-pro-max.css.

Main correction: maroon/oxblood is reduced to a tiny atmospheric accent. Graphite + slate dominate, bronze is accent/CTA, ivory is text.
Homepage is replaced with a cleaner AI-native luxury landing experience.
