const required=["NEXT_PUBLIC_APP_URL"]; const missing=required.filter(k=>!process.env[k]); if(missing.length){console.error(`Missing production variables: ${missing.join(", ")}`);process.exit(1)} if(process.env.EXECUTION_LIVE_ENABLED==="true"&&!process.env.EXECUTION_WORKER_URL){console.error("Live execution requires EXECUTION_WORKER_URL");process.exit(1)} console.log("Production environment gates passed");
