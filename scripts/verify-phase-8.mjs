import fs from "node:fs";import path from "node:path";const required=["src/types/market-data.ts","src/lib/market-data/core/provider.ts","src/lib/market-data/candles/aggregate.ts","src/services/market-data-orchestrator.ts","src/app/admin/market-data/page.tsx","docs/market-data/phase-8.md"];const missing=required.filter(file=>!fs.existsSync(path.resolve(file)));if(missing.length){console.error("Phase 8 missing files",missing);process.exit(1);}console.log(`Phase 8 structural verification passed (${required.length} critical files).`);