# Phase 17 Visual CMS
Replaces the old in-memory placeholder builder with Supabase-backed visual pages and blocks. Admin can create pages, add blocks, drag/reorder, edit headings/content/buttons, show/hide and publish without editing JSON. Home and pricing CMS records are created as drafts so current production pages are not silently broken.
